# CExP Hub SDK

This repository is a TypeScript SDK scaffold for the CExP Hub browser facade (`window.CExP`).

## Development

- `npm run build` to create bundles.
- `npm run test` to run unit tests (currently none; vitest is configured to pass).

